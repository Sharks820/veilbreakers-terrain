name: Type Check

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["main", "master"]

jobs:
  pyright:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install veilbreakers-mcp from source
        run: |
          pip install "git+https://github.com/Sharks820/veilbreakers-gamedev-toolkit.git@1516c802c487c74a0f4e4aec42d8e42930d75e24#subdirectory=Tools/mcp-toolkit"

      - name: Install project with dev extras
        run: pip install -e ".[dev]"

      - name: Install pyright
        run: pip install pyright

      - name: Run pyright type check
        run: pyright -p pyrightconfig.json
